###
# kubernetes proxy config

# default config should be adequate

# Add your own!
KUBE_PROXY_ARGS="--bind-address={CURRENT_IP} --hostname-override={CURRENT_IP} --cluster-cidr={SERVICE_CIDR} --kubeconfig=/etc/kubernetes/kube-proxy.kubeconfig"
